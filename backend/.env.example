# ================================
# Buddy-AI 环境变量配置模板
# ================================
#
# 使用方法：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 填写实际的 API Key 和配置信息
# 3. 根据需要修改环境配置
# ================================

# ---------- 应用配置 ----------
APP_NAME=Buddy-AI
APP_VERSION=2.0.0
DEBUG=true
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000

# ---------- API Keys (必填) ----------
# 阿里云 DashScope API Key (用于 Qwen 模型和 Embedding)
# 获取地址: https://dashscope.aliyun.com/
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# Tavily Search API Key (用于网络搜索)
# 获取地址: https://tavily.com/
TAVILY_API_KEY=your_tavily_api_key_here

# 可选：OpenAI API Key (如需使用 OpenAI 模型)
OPENAI_API_KEY=

# ---------- 数据库配置 ----------
# Redis 连接 (用于会话 checkpoint)
REDIS_URL=redis://localhost:6379/0

# PostgreSQL 连接 (用于记忆存储)
POSTGRESQL_URL=postgresql://postgres:postgres@localhost:5432/buddyai

# ---------- 向量数据库配置 ----------
CHROMA_PERSIST_DIR=./backend/chroma_db
CHROMA_COLLECTION_NAME=buddy_ai_knowledge

# ---------- 文件上传配置 ----------
UPLOAD_DIR=./backend/uploads
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=pdf,docx,txt,md,csv

# ---------- LLM 配置 ----------
LLM_MODEL=qwen-plus
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000
LLM_STREAM=true

# ---------- Embedding 配置 ----------
EMBEDDING_MODEL=text-embedding-v2
EMBEDDING_DIMENSIONS=1536

# ---------- Agent 配置 ----------
MAX_TOOL_CALLS=3
MAX_RETRIES=3

# ---------- WebSocket 配置 ----------
WS_HEARTBEAT_INTERVAL=30
WS_MAX_CONNECTIONS=100

# ---------- 安全配置 ----------
# 生产环境必须修改此密钥
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ---------- 日志配置 ----------
LOG_LEVEL=INFO

# ---------- 前端配置 ----------
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
